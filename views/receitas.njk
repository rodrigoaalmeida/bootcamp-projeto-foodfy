{% extends "layout.njk" %}

{% block head %}
    <title>Foodfy - Home</title>
{% endblock %}

{% block conteudo %}
    <div class="conteudo">
        <div class="cards">
            {% for item in itens %}
                <div class="card">
                    <img src="{{ item.imageLocal }}" alt="Imagem do {{ item.title }}">
                    <h4>{{ item.title }}</h4>
                    <p>por
                        {{ item.author }}</p>
                </div>
            {% endfor %}
            {% for item in itens %}
                <div class="card">
                    <img src="{{ item.imageLocal }}" alt="Imagem do {{ item.title }}">
                    <h4>{{ item.title }}</h4>
                    <p>por
                        {{ item.author }}</p>
                </div>
            {% endfor %}

        </div>
    </div>

    <script src="scripts.js"></script>

{% endblock %}